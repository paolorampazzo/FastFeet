import { createBrowserHistory } from 'history';

const history = createBrowserHistory({ forceRefresh: true });
// export const historyRefresh = createBrowserHistory({ forceRefresh: true });

export default history;
